# ====== ENV contoh untuk deploy ke Vercel ======
# Salin ke Vercel (Project Settings → Environment Variables)
# atau copy ke file .env lokal jika diperlukan.

# Auth JWT (WAJIB)
JWT_SECRET=REPLACE_WITH_LONG_RANDOM_STRING

# Base URL aplikasi (Production)
NEXTAUTH_URL=https://pgriokutimur.org

# Optional (tak dipakai langsung, tapi aman diisi)
NEXTAUTH_SECRET=REPLACE_WITH_RANDOM_STRING

# Database Postgres (diisi setelah membuat DB Neon/Supabase)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Email (opsional; diperlukan fitur lupa password)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your@gmail.com
EMAIL_PASS=YOUR_GOOGLE_APP_PASSWORD

# Catatan:
# - Jangan commit file .env, sudah di-ignore.
# - Untuk Preview di Vercel, NEXTAUTH_URL bisa diisi domain preview.